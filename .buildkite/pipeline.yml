steps:
  - label: ":arch: aur-pkgbuild-update"
    agents:
      queue: "default"
    image: "ghcr.io/carteramesh/docker/aur-builder:latest"
    commands:
      - id
      - echo $PWD
      - ls -ld $PWD
      - ./pkgbuild-update.sh
