name: aur-check
on:
  workflow_dispatch:

jobs:
  discover-packages:
    name: Discover packages
    runs-on: aur-runners
    outputs:
      packages: ${{ steps.packages.outputs.packages }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Export packages matrix JSON
        id: packages
        shell: bash
        run: |
          {
            echo 'packages<<EOF'
            cat packages.json
            echo EOF
          } >> "$GITHUB_OUTPUT"

  check-and-verify:
    name: Check and verify (${{ matrix.package.name }})
    runs-on: aur-runners
    needs: discover-packages
    strategy:
      fail-fast: false
      matrix:
        package: ${{ fromJSON(needs.discover-packages.outputs.packages) }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Check Dagger CLI availability
        run: dagger version
      - name: Check upstream and verify PKGBUILD sources
        env:
          PKGNAME: ${{ matrix.package.name }}
          NVCHECKER_NAME: ${{ matrix.package.nvcheckerName || '' }}
          DAGGER_CLOUD_TOKEN: ${{ secrets.DAGGER_CLOUD_TOKEN }}
        shell: bash
        run: |
          eval "$(ssh-agent -s)"
          trap 'ssh-agent -k' EXIT
          ssh-add /home/runner/.ssh/id_ed25519

          args=(check-and-verify-package --pkgname="$PKGNAME" --sock="$SSH_AUTH_SOCK")

          if [[ -n "$NVCHECKER_NAME" ]]; then
            args+=(--nvchecker-name="$NVCHECKER_NAME")
          fi

          dagger call "${args[@]}"
